% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{alpine_field}
\alias{alpine_field}
\title{Alpine.js Form Field Component}
\usage{
alpine_field(
  label = NULL,
  input_tag,
  model_var,
  error_var = NULL,
  label_style = NULL,
  error_style = NULL,
  input_class = NULL,
  container_class = NULL,
  ...
)
}
\arguments{
\item{label}{Character string or htmltools tag for the field label.}

\item{input_tag}{An htmltools tag (typically \code{input()} or \code{textarea()})
to use as the input element. The tag should not have \code{x-model} already
applied; this function will add it.}

\item{model_var}{Character string specifying the state variable for
two-way binding. Example: \code{"emailAddress"}, \code{"userAge"}.}

\item{error_var}{Optional character string specifying the error message
variable. If \code{NULL} (default), no error div is rendered. If provided,
the error div will show when this variable is non-empty/truthy.
Example: \code{"emailError"}.}

\item{label_style}{Optional character string with inline CSS for the label.}

\item{error_style}{Optional character string with inline CSS for the error message.
Default colors and styling are applied if not specified.}

\item{input_class}{Optional CSS class(es) for the input element.}

\item{container_class}{Optional CSS class(es) for the outer container div.}

\item{...}{Additional attributes passed to the input tag (e.g., modifiers
like \code{.lazy = TRUE} or \code{.debounce = 300} for \code{alpine_model}).
Note: These are passed to \code{alpine_model()}, not to the tag constructor.}
}
\value{
An htmltools tag object representing the field container with
label (if provided), input, and optional error div. Pipe-compatible.
}
\description{
Creates a labeled input field with optional error message display,
using two-way data binding via \code{alpine_model()} and conditional error
visibility via \code{alpine_show()} and \code{alpine_text()}.
}
\details{
Component contains label, input, and optional error div.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{htmltools::tags$div(
  alpine_field(
    label = "Email",
    input_tag = htmltools::tags$input(type = "email", placeholder = "name@example.com"),
    model_var = "email",
    error_var = "emailError"
  ),
  htmltools::tags$button("Submit")
) |>
  alpine_data(
    email = "",
    emailError = "",
    validate = htmlwidgets::JS("() => \{
      if (!this.email.includes('@')) \{
        this.emailError = 'Invalid email address';
      \} else \{
        this.emailError = '';
      \}
    \}")
  )
}\if{html}{\out{</div>}}

\strong{Styling Customization:}
\itemize{
\item Use \code{label_style} to customize the label appearance
\item Use \code{error_style} to customize error text color and styling
\item Use \code{input_class} to add CSS classes to the input (e.g., Tailwind, Bootstrap)
\item Use \code{container_class} to style the overall field container
}
}
\examples{
\donttest{
alpine_field(
  label = "Name",
  input_tag = htmltools::tags$input(type = "text", placeholder = "Enter name"),
  model_var = "userName"
)
}

}
\seealso{
Other UI Components: 
\code{\link{alpine_accordion}()},
\code{\link{alpine_button}()},
\code{\link{alpine_filter_buttons}()},
\code{\link{alpine_modal}()},
\code{\link{alpine_stat_card}()},
\code{\link{alpine_tabs}()},
\code{\link{alpine_value_display}()}
}
\concept{UI Components}
