% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_data_js}
\alias{alpine_data_js}
\title{Initialize reactive state with raw JavaScript}
\usage{
alpine_data_js(tag, js_string)
}
\arguments{
\item{tag}{An htmltools tag object.}

\item{js_string}{Character string containing a JavaScript expression.
Can be:
\itemize{
\item A reference to a global object: \code{"window.myApp"} or \code{"globalState"}
\item An inline object literal: \code{"{count: 0, name: 'test', toggle: () => ...}"}
\item Any valid JavaScript expression that evaluates to an object
}}
}
\value{
The modified tag with \code{x-data} attribute.
}
\description{
Creates an \code{x-data} attribute directly from a JavaScript expression string,
without automatic serialization. Use when you need complex objects with methods,
references to global objects, or inline JavaScript patterns that R serialization
can't handle.
}
\details{
Use for complex objects, global references, or inline methods.
\code{alpine_data_js()} takes raw JavaScript as-is. This is useful when:
\itemize{
\item The object has methods (functions) that shouldn't be stringified
\item You're using a global object defined elsewhere in your app
\item You need templates, computed properties, or other JS patterns
}

\strong{Escaping:} The JS string is NOT escaped or validated. Ensure it's
valid JavaScript and properly quoted for HTML attributes.
}
\examples{
\donttest{
# Reference a global object
htmltools::tags$div(
  htmltools::tags$button("Reset") |> alpine_on("click", "app.reset()")
) |>
  alpine_data_js("window.app")

# Inline object with methods
htmltools::tags$div(
  htmltools::tags$button("Toggle") |> alpine_on("click", "toggle()"),
  htmltools::tags$div("Open") |> alpine_show("isOpen")
) |>
  alpine_data_js("{
    isOpen: false,
    toggle() { this.isOpen = !this.isOpen }
  }")

# Combine with tags$script for definition
htmltools::tags$div(
  htmltools::tags$script("window.calculator = {
    result: 0,
    add(n) { this.result += n },
    clear() { this.result = 0 }
  }"),
  htmltools::tags$button("Add 5") |> alpine_on("click", "add(5)"),
  htmltools::tags$span() |> alpine_text("result")
) |>
  alpine_data_js("window.calculator")
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_for}()},
\code{\link{alpine_html}()},
\code{\link{alpine_id}()},
\code{\link{alpine_if}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_teleport}()},
\code{\link{alpine_text}()},
\code{\link{alpine_transition}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
