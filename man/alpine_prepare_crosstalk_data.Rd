% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstalk.R
\name{alpine_prepare_crosstalk_data}
\alias{alpine_prepare_crosstalk_data}
\title{Prepare Data for Crosstalk Integration}
\usage{
alpine_prepare_crosstalk_data(data, key_column = NULL, group = NULL)
}
\arguments{
\item{data}{A data.frame to prepare.}

\item{key_column}{Character string naming the column to use as row identifiers
for Crosstalk selection/filtering. If NULL, row numbers are used.}

\item{group}{Character string naming the Crosstalk group (e.g., "iris_data").
This group name must match across all widgets that should be synchronized.}
}
\value{
A list containing:
\itemize{
\item \code{keys}: Character vector of row identifiers (Crosstalk keys)
\item \code{data}: R data structure ready for JSON serialization
\item \code{group}: The group name (passed through)
\item \code{key_column}: The name of the key column (for JS reference)
}
}
\description{
Extracts and prepares R data for use with Crosstalk SharedData,
handling key extraction, grouping, and JSON serialization for Alpine.js
consumption.
}
\details{
\strong{Key Extraction:}
If \code{key_column} is specified, values from that column are used as row keys.
Otherwise, row numbers (1, 2, 3, ...) are converted to characters.

\strong{Group Names:}
Group names should be unique, meaningful identifiers. Examples:
\itemize{
\item "iris_species_table"
\item "mtcars_selection"
\item "housing_data_1"
}

The same group name must be used across all widgets that share selections.
}
\examples{
\donttest{
prepared <- alpine_prepare_crosstalk_data(
  iris,
  key_column = "Species",
  group = "iris_demo"
)
}

}
\seealso{
Other Crosstalk Integration: 
\code{\link{alpine_crosstalk_table}()},
\code{\link{alpine_has_crosstalk}()}
}
\concept{Crosstalk Integration}
