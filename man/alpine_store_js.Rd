% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magics.R
\name{alpine_store_js}
\alias{alpine_store_js}
\title{Access a global Alpine store value}
\usage{
alpine_store_js(store_name, property = NULL)
}
\arguments{
\item{store_name}{Character string. The name of the store (as passed to
\code{alpine_global_store()}).}

\item{property}{Optional character string. A property path within the store
(e.g., \code{"on"}, \code{"theme"}, \code{"user.name"}). If \code{NULL}, returns the store root.}
}
\value{
An \code{htmlwidgets::JS} object containing the \verb{$store.name[.property]} expression.
}
\description{
Generates a \verb{$store.name} or \verb{$store.name.property} JavaScript expression for
reading from or writing to a global Alpine store registered with
\code{alpine_global_store()}. Use the result in directives, event handlers, and
expressions throughout any Alpine component on the page.
}
\details{
Store access expressions can be used anywhere Alpine expressions are accepted:
\itemize{
\item In conditions: \code{alpine_show(div, alpine_store_js("app", "isLoggedIn"))}
\item In event handlers: \code{alpine_on(btn, "click", "$store.darkMode.on = !$store.darkMode.on")}
}

Unlike local component data, stores are globally accessible across all Alpine
components on the page.
}
\examples{
\donttest{
# Access store root
alpine_store_js("darkMode")

# Access a store property
alpine_store_js("darkMode", "on")
}

}
\seealso{
Other Magic Properties: 
\code{\link{alpine_dispatch}()},
\code{\link{alpine_el}()},
\code{\link{alpine_next_tick}()},
\code{\link{alpine_refs}()},
\code{\link{alpine_watch}()}
}
\concept{Magic Properties}
