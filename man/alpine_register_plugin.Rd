% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plugins.R
\name{alpine_register_plugin}
\alias{alpine_register_plugin}
\title{Register an Alpine.js plugin for use with alpinejs}
\usage{
alpine_register_plugin(
  name,
  version = NULL,
  npm_scope = "alpinejs",
  required_alpine_version = "3.0",
  init_code = NULL
)
}
\arguments{
\item{name}{Character string naming the plugin (e.g., "persist", "mask").}

\item{version}{Character string specifying the plugin version. Must match
the Alpine.js version (e.g., "3.15.8"). Default: uses installed Alpine.js
version from \code{getOption("alpinejs.version")}.}

\item{npm_scope}{Character string for the npm package scope (default: "alpinejs").
Used to construct CDN URLs. For example, "persist" with "alpinejs" scope
becomes \verb{@alpinejs/persist}.}

\item{required_alpine_version}{Character string specifying the minimum required
Alpine.js version. Default: "3.0" (supports Alpine 3.x).}

\item{init_code}{Optional character string containing JavaScript code to
execute when the plugin initializes. Useful for configuration.}
}
\value{
Invisibly returns \code{TRUE} if registration succeeds. Used for its
side effect of adding the plugin to the registry.
}
\description{
Adds a plugin to the alpinejs registry. Plugins are JavaScript extensions from the
Alpine.js ecosystem (e.g., persist, mask, sort, etc.). Once registered, setup functions
like \code{alpine_setup_mask()} can use the plugin and its dependencies will be automatically
injected into generated HTML.
}
\details{
\strong{Plugin Registry:} Plugins are stored in \code{options("alpinejs.plugins")} as a
named list. Each entry contains metadata used to generate CDN URLs and
manage dependencies.

\strong{CDN URLs:} Plugin dependencies are automatically constructed as:

\if{html}{\out{<div class="sourceCode">}}\preformatted{https://cdn.jsdelivr.net/npm/@\{npm_scope\}/\{name\}@\{version\}/dist/\{name\}.min.js
}\if{html}{\out{</div>}}

\strong{Duplicate Detection:} If a plugin with the same name is already registered,
a warning is issued and the existing registration is kept (no re-registration).

\strong{Version Validation:} Plugin versions must be SemVer format (e.g., "3.15.8").
Invalid versions produce a warning but don't prevent registration.
}
\examples{
\donttest{
alpine_register_plugin("persist", version = "3.15.8")
alpine_is_plugin_registered("persist")
}

}
\seealso{
Other Plugin System: 
\code{\link{alpine_get_plugin_registry}()},
\code{\link{alpine_is_plugin_registered}()},
\code{\link{plugin_templates}}
}
\concept{Plugin System}
