% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-constructors.R
\name{alpine_object}
\alias{alpine_object}
\title{Create a JavaScript object for Alpine.js state}
\usage{
alpine_object(...)
}
\arguments{
\item{...}{Named arguments representing properties of the JavaScript object.
Values are automatically serialized to JSON. Supports:
\itemize{
\item Character strings (quoted in JSON)
\item Numeric values (integers and floats)
\item Logical values (converted to true/false)
\item NULL (converted to null)
\item Lists as nested objects
\item \code{htmlwidgets::JS()} expressions for raw JavaScript injection
}}
}
\value{
An \code{htmlwidgets::JS} object containing a JavaScript object literal.
Suitable for use as an initial value in \code{alpine_data()}.
}
\description{
Helper that builds a JavaScript object literal from R named arguments,
without writing raw JavaScript strings. Automatically serializes R values to JSON.
}
\details{
This function provides a semantic alternative to multi-line JavaScript strings
for defining Alpine component objects. Use R syntax to define objects:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Before
settings = htmlwidgets::JS("\{
  username: 'john_doe',
  email: 'john@example.com',
  theme: 'light'
\}")

# After
settings = alpine_object(
  username = "john_doe",
  email = "john@example.com",
  theme = "light"
)
}\if{html}{\out{</div>}}

For raw JavaScript expressions (like computed properties), pass them
wrapped in \code{htmlwidgets::JS()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{alpine_object(
  timestamp = htmlwidgets::JS("Date.now()"),
  isActive = TRUE
)
}\if{html}{\out{</div>}}
}
\examples{
\donttest{
htmltools::tags$div(
  htmltools::tags$input(type = "text", placeholder = "Username") |> alpine_model("user.name"),
  htmltools::tags$input(type = "email", placeholder = "Email") |> alpine_model("user.email"),
  htmltools::tags$input(type = "number", placeholder = "Font size") |> alpine_model("settings.fontSize", .number = TRUE)
) |>
  alpine_data(
    user = alpine_object(
      name = "Alice",
      email = "alice@example.com",
      premium = TRUE
    ),
    settings = alpine_object(
      theme = "light",
      fontSize = 16
    )
  )
}

}
\seealso{
Other JavaScript Helpers: 
\code{\link{alpine_array}()}
}
\concept{JavaScript Helpers}
