% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_model}
\alias{alpine_model}
\title{Two-way data binding for form inputs}
\usage{
alpine_model(
  tag,
  variable,
  .lazy = FALSE,
  .debounce = NULL,
  .number = FALSE,
  .trim = FALSE,
  .cssSelector = NULL
)
}
\arguments{
\item{tag}{An htmltools tag object, typically an \verb{<input>}, \verb{<textarea>},
or \verb{<select>} element.}

\item{variable}{Character string specifying the state variable name to bind
to (e.g., "searchText", "selectedOption").}

\item{.lazy}{Logical. If \code{TRUE}, updates only on blur/change events instead
of on every input. Useful for validation or expensive operations.}

\item{.debounce}{Numeric or \code{NULL}. Debounces updates by specified milliseconds.}

\item{.number}{Logical. If \code{TRUE}, converts input value to number.}

\item{.trim}{Logical. If \code{TRUE}, removes leading/trailing whitespace.}

\item{.cssSelector}{Optional CSS selector for targeting a nested element.}
}
\value{
The modified tag with \code{x-model} attribute appended.
}
\description{
Creates an \code{x-model} attribute that synchronizes a form input with a state variable.
When the user changes the input, Alpine.js automatically updates the state variable.
When the state variable changes, Alpine.js automatically updates the input display.
}
\details{
\strong{Input Types:}
\itemize{
\item Text inputs (\verb{<input type="text">}): binds to string value
\item Checkboxes (\verb{<input type="checkbox">}): binds to boolean
\item Radio buttons: binds to selected value
\item Selects (\verb{<select>}): binds to (first) selected option value
\item Textareas: binds to text content
\item Multiple selects: binds to array of selected values
}

\strong{Modifier Syntax:} Modifiers are applied in the correct Alpine.js order:
\code{.lazy} -> \code{.debounce.NNN} -> \code{.number} -> \code{.trim}. This means an expression
\code{alpine_model(input(), "count", .lazy = TRUE, .debounce = 300, .number = TRUE)}
produces \code{x-model.lazy.debounce.300.number="count"}.

\strong{Modifier Combinations:} Modifiers can be combined. Common patterns:
\itemize{
\item \code{.lazy = TRUE}: For form validation (update only on blur)
\item \code{.debounce = 300}: For search inputs (avoid queries on every keystroke)
\item \code{.number = TRUE}: For numeric inputs without type="number"
\item \code{.lazy + .debounce}: For expensive validation (throttle + debounce)
\item \code{.debounce + .number}: For numeric search/filter inputs
}

\strong{Performance:} Debouncing is especially useful for inputs that trigger
expensive operations like API calls, regex searches, or large dataset
filtering. Recommended starting values: 300-500ms for user typing.
}
\examples{
\donttest{
htmltools::tags$input(type = "text", placeholder = "Enter name") |>
  alpine_model("userName")
}

}
\concept{Form Input}
