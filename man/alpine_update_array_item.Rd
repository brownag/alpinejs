% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-events.R
\name{alpine_update_array_item}
\alias{alpine_update_array_item}
\title{Update Array Item Property}
\usage{
alpine_update_array_item(
  array,
  id_param = "id",
  property = "value",
  value_param = "newValue"
)
}
\arguments{
\item{array}{Character: path to the array property (e.g., \code{"products"})}

\item{id_param}{Character: name of the parameter for item ID (e.g., \code{"id"})}

\item{property}{Character: property to update (e.g., \code{"quantity"})}

\item{value_param}{Character: name of the parameter for new value (default: \code{"newValue"})}
}
\value{
A character string containing JavaScript code for updating.
Generate the full function with sprintf wrapper.
}
\description{
Helper to create a JavaScript fragment that updates a property of an item
in an array by ID.
}
\details{
Generates code like:

\if{html}{\out{<div class="sourceCode javascript">}}\preformatted{const item = this.products.find(p => p.id === id);
if (item && newValue >= 0) item.quantity = newValue;
}\if{html}{\out{</div>}}
}
\examples{
\donttest{
htmltools::tags$div(
  htmltools::tags$ul(
    alpine_for(
      htmltools::tags$li(
        htmltools::tags$span() |> alpine_text("item.quantity"),
        htmltools::tags$input(type = "number") |> alpine_model("newQty", .number = TRUE),
        htmltools::tags$button("Update") |>
          alpine_on("click",
            sprintf("function() { \%s; if (newQty > 0) item.quantity = newQty; }",
              alpine_update_array_item("products", "id", "quantity"))
          )
      ),
      "item in products",
      key = "item.id"
    )
  )
) |>
  alpine_data(
    products = list(list(id = 1, quantity = 5)),
    newQty = 0
  )
}

}
\seealso{
Other Array Operations: 
\code{\link{alpine_add_to_array}()},
\code{\link{alpine_remove_from_array}()}
}
\concept{Array Operations}
