% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstalk.R
\name{alpine_add_crosstalk_listener}
\alias{alpine_add_crosstalk_listener}
\title{Add Crosstalk Event Listener to Alpine Component}
\usage{
alpine_add_crosstalk_listener(
  tag,
  group,
  handle_type = "both",
  key_column = "id"
)
}
\arguments{
\item{tag}{An htmltools tag object (typically a div or table).}

\item{group}{Character string naming the Crosstalk group.}

\item{handle_type}{Character string: "selection" or "filter" or "both".}

\item{key_column}{Character string naming the data column used as Crosstalk key
(for proper row matching). Default: "id" (assumes x-data.id contains row keys).}
}
\value{
The tag with injected Crosstalk listener JavaScript.
}
\description{
Injects JavaScript code into an Alpine.js component to listen for Crosstalk
selection and filter events, updating the component's state when other widgets
change their selection.
}
\details{
\strong{Handle Types:}
\itemize{
\item "selection" -- Responds to single-item brushing/selection
\item "filter" -- Responds to multi-item filtering (checkbox, range, etc.)
\item "both" -- Listens to both selection and filter events
}

\strong{Integration:}
The component's Alpine x-data must include:
\itemize{
\item \verb{selectedKeys: []} -- Array of selected row keys
\item \verb{filteredKeys: []} -- Array of filtered row keys
\item \verb{allData: [...]} -- Complete dataset
}

\strong{Event Prevention:}
The listener automatically prevents circular selection loops by checking
\verb{if (e.sender !== handle)} before updating state.
}
\keyword{internal}
