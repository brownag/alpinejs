% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_for}
\alias{alpine_for}
\title{Repeat an element for each array or object item}
\usage{
alpine_for(inner_tag, expression, key = NULL)
}
\arguments{
\item{inner_tag}{An htmltools tag to repeat. This tag will be the template
for each iteration.}

\item{expression}{Character string containing the iteration expression
(e.g., \code{"item in items"}, \code{"(value, key) in object"}, \code{"(item, index) in array"}).
The variable(s) defined here are available inside the repeated element.}

\item{key}{Optional character string specifying a unique key for each item.
Example: \code{"item.id"}. Keys help Alpine maintain element identity when
the list changes, improving performance and state preservation.
If \code{NULL}, no \code{x-key} attribute is added.}
}
\value{
An htmltools \verb{<template>} tag with \verb{x-for} and optionally \code{x-key}
attributes applied. Automatically includes the Alpine.js dependency.
The template can be used within an \code{alpine_data()} component.
}
\description{
Creates an Alpine.js loop using \verb{<template>} with \verb{x-for} and optional \code{x-key} attributes.
For each item, Alpine.js renders a copy of the template element using the iteration variables.
}
\details{
\strong{Template Syntax:} Alpine uses \verb{<template>} tags to wrap looped content.
The browser doesn't render the \verb{<template>} tag itself, only the repeated
content inside it.

\strong{Iteration Variables:} The variable(s) in the expression are available
in the scope of nested elements, but not outside the template.

\strong{Keys:} Provide a \code{key} whenever your list items have a unique identifier
(e.g., database ID, UUID). This allows Alpine to:
\itemize{
\item Match DOM elements to data items when the list reorders
\item Preserve component state and input values during updates
\item Improve performance by avoiding full re-renders
}

\strong{Common Patterns:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Array iteration
alpine_for(alpine_tag("div", alpine_tag("p") |> alpine_text("item.name")), "item in items", key = "item.id")

# Array with index
alpine_for(alpine_tag("span") |> alpine_text("index"), "(item, index) in items", key = "item.id")

# Object iteration
alpine_for(alpine_tag("div", alpine_tag("span") |> alpine_text("value")), "(value, key) in config")
}\if{html}{\out{</div>}}
}
\examples{
\donttest{
alpine_for(
  htmltools::tags$div(htmltools::tags$span() |> alpine_text("item.name")),
  "item in items",
  key = "item.id"
)
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_data_js}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_html}()},
\code{\link{alpine_id}()},
\code{\link{alpine_if}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_teleport}()},
\code{\link{alpine_text}()},
\code{\link{alpine_transition}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
