% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-computed.R
\name{alpine_sum_filtered}
\alias{alpine_sum_filtered}
\title{Computed Property: Sum Filtered Items}
\usage{
alpine_sum_filtered(property, condition, amount_field)
}
\arguments{
\item{property}{Character string: name of the array property to filter}

\item{condition}{Character string: filter condition using \code{x} as the item variable}

\item{amount_field}{Character string: name of the field to sum}
}
\value{
An \code{htmlwidgets::JS} object containing a JavaScript function that
returns a sum when called.
}
\description{
Helper to create a computed property that sums a field across filtered items.
Combines filter and reduce operations into a single computed function.
}
\details{
This helper generates a function equivalent to:

\if{html}{\out{<div class="sourceCode javascript">}}\preformatted{function() \{
  return this.\{property\}
    .filter(x => \{condition\})
    .reduce((sum, x) => sum + x.\{amount_field\}, 0);
\}
}\if{html}{\out{</div>}}
}
\examples{
\donttest{
htmltools::tags$div() |>
  alpine_data(
    transactions = list(
      list(type = "income", amount = 1000),
      list(type = "expense", amount = 250),
      list(type = "income", amount = 500)
    ),
    totalIncome = alpine_sum_filtered("transactions", "x.type === 'income'", "amount"),
    totalExpense = alpine_sum_filtered("transactions", "x.type === 'expense'", "amount"),
    balance = alpine_compute("this.totalIncome() - this.totalExpense()")
  )
}

}
\seealso{
Other Computed Properties: 
\code{\link{alpine_combine_conditions}()},
\code{\link{alpine_compute}()},
\code{\link{alpine_count_filtered}()},
\code{\link{alpine_multi_condition}()}
}
\concept{Computed Properties}
