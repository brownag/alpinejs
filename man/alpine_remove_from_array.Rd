% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-events.R
\name{alpine_remove_from_array}
\alias{alpine_remove_from_array}
\title{Remove Item from Array}
\usage{
alpine_remove_from_array(array, identifier = "id", is_param = FALSE)
}
\arguments{
\item{array}{Character: path to the array property (e.g., \code{"products"}, \code{"todos"})}

\item{identifier}{Character: property/parameter to match (e.g., \code{"id"}, or a parameter
like \code{"itemId"} if the function takes a parameter)}

\item{is_param}{Logical: if TRUE, \code{identifier} is a function parameter;
if FALSE, it's a property to match by ID. Default: FALSE}
}
\value{
A character string containing JavaScript code for removal.
Use within a function or event handler.
}
\description{
Helper to create a JavaScript fragment that removes an item from an array
by ID or index.
}
\details{
Generates code like:

\if{html}{\out{<div class="sourceCode javascript">}}\preformatted{this.products = this.products.filter(p => p.id !== id);
}\if{html}{\out{</div>}}

If \code{is_param=FALSE}, wraps in a function that accepts the identifier.
}
\examples{
\donttest{
htmltools::tags$div(
  htmltools::tags$ul(
    alpine_for(
      htmltools::tags$li(
        htmltools::tags$span() |> alpine_text("item.name"),
        htmltools::tags$button("Delete") |>
          alpine_on("click", sprintf("function() { \%s }", 
            alpine_remove_from_array("products", "item.id", is_param = FALSE)))
      ),
      "item in products",
      key = "item.id"
    )
  )
) |>
  alpine_data(
    products = list(
      list(id = 1, name = "Product A"),
      list(id = 2, name = "Product B")
    )
  )
}

}
\seealso{
Other Array Operations: 
\code{\link{alpine_add_to_array}()},
\code{\link{alpine_update_array_item}()}
}
\concept{Array Operations}
