% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{alpine_stat_card}
\alias{alpine_stat_card}
\title{Alpine.js Stat Card Component}
\usage{
alpine_stat_card(
  label,
  value_expr,
  bg_color = "#f5f5f5",
  text_color = "#333",
  accent_color = NULL,
  border_style = NULL
)
}
\arguments{
\item{label}{Character string or htmltools tag for the label text
(e.g., "Total Users", "Revenue").}

\item{value_expr}{Character string containing a JavaScript expression
that evaluates to the displayed value. Can be a simple state variable
(e.g., \code{"count"}) or a computed expression (e.g., \code{"items.length"}).}

\item{bg_color}{Character string with the card background color
(hex, rgb, or color name). Default: \code{"#f5f5f5"} (light gray).}

\item{text_color}{Character string with the main text color (label and borders).
Default: \code{"#333"} (dark gray).}

\item{accent_color}{Optional character string for accent elements
(e.g., value text, borders). If \code{NULL}, uses the same as \code{text_color}.}

\item{border_style}{Optional character string for CSS border styling.
If \code{NULL}, no border is used. Example: \code{"1px solid #ddd"}.}
}
\value{
An htmltools tag object representing the stat card.
Pipe-compatible.
}
\description{
Creates a colored stat card displaying a label and a reactive value.
The value is rendered via \code{alpine_text()} and updates reactively as the
underlying state changes.
}
\details{
The card is a styled div with two text elements:
\itemize{
\item A label line
\item A large value line (using \code{x-text} for reactivity)
}

\strong{Styling:} Minimal default styling is applied. Use the color parameters
to customize. For more complex styling, wrap the component in a styled
container or use the class parameter.

\strong{Value Updates:} Because the value is rendered via \code{alpine_text()}, it
automatically updates whenever the underlying state variable changes.
This is useful for dashboard cards, metrics, and counters.
}
\examples{
\donttest{
alpine_stat_card(
  label = "Total Users",
  value_expr = "userCount",
  bg_color = "#e3f2fd"
) |>
  alpine_data(userCount = 42)
}

}
\seealso{
Other UI Components: 
\code{\link{alpine_accordion}()},
\code{\link{alpine_button}()},
\code{\link{alpine_field}()},
\code{\link{alpine_filter_buttons}()},
\code{\link{alpine_modal}()},
\code{\link{alpine_tabs}()},
\code{\link{alpine_value_display}()}
}
\concept{UI Components}
