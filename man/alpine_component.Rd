% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\docType{class}
\name{alpine_component}
\alias{alpine_component}
\title{Alpine Component Class}
\description{
The \code{alpine_component} S3 class is automatically assigned to all alpinejs
functions that return HTML elements. This enables special handling in
different contexts (console, vignettes, RStudio Viewer) through S3 method
dispatch.
}
\details{
\subsection{Automatic Assignment}{

The class is automatically assigned to outputs from:
\itemize{
\item \code{alpine_tag()} - Base HTML tag creation
\item Modifier functions: \code{alpine_data()}, \code{alpine_on()}, \code{alpine_show()},
\code{alpine_bind()}, \code{alpine_model()}, \code{alpine_text()}, \code{alpine_for()},
\code{alpine_init()}, \code{alpine_data_js()}
\item Component functions: \code{alpine_accordion()}, \code{alpine_button()},
\code{alpine_modal()}, \code{alpine_tabs()}, etc.
\item Helper functions that return HTML
}
}

\subsection{S3 Methods}{

Users can interact with \code{alpine_component} objects through these S3 methods:
\itemize{
\item \code{print(x)} - Print raw HTML to console
\item \code{knit_print(x, ...)} - Render in R Markdown. Wraps HTML in interactive
JavaScript widget to preserve camelCase attributes during pkgdown build.
\item \code{alpine_browser(x)} - Explicitly open in RStudio Viewer for interactive preview
}
}

\subsection{Why This Class Exists}{

The Alpine.js library requires exact attribute name casing (e.g.,
\code{x-bind:textContent} must not become \code{x-bind:textcontent}). However,
pkgdown's underlying HTML parser (libxml2) lowercases all attributes.

The \code{alpine_component} class enables automatic protection: when rendered
in vignettes, components are wrapped in a JavaScript shell that:
\enumerate{
\item Hides the HTML from pkgdown's parser
\item Unwraps and renders interactively after the page loads
\item Preserves all camelCase attributes exactly
}

This protection is transparent to users—no code changes needed.
}
}
\examples{
\donttest{
# Create an alpine component (class is automatic)
component <- alpine_tag("div", "Hello")
class(component)  # Returns: c("alpine_component", "shiny.tag", ...)

# Print to console
print(component)

# Open in RStudio Viewer for interactive preview
alpine_browser(component)
}

}
\keyword{internal}
