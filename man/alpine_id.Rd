% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_id}
\alias{alpine_id}
\title{Define a unique ID scope for a group of related elements}
\usage{
alpine_id(tag, ..., .cssSelector = NULL)
}
\arguments{
\item{tag}{An htmltools tag object.}

\item{...}{One or more character strings naming the ID scopes. Each name is
used as a prefix when generating IDs with \verb{$id('name')}.}

\item{.cssSelector}{Optional CSS selector for targeting a nested element.}
}
\value{
The modified tag with \code{x-id} attribute appended.
}
\description{
Creates an \code{x-id} attribute that establishes a scope for generating unique IDs
with the \verb{$id()} magic property. This allows multiple instances of the same
component to have unique, non-conflicting IDs for accessibility (e.g., linking
labels to inputs).
}
\details{
Within the component, use \verb{$id('scope-name')} to generate a unique ID for
that scope. Alpine.js ensures that each component instance gets a different
numeric suffix (e.g., \code{tooltip-button-1}, \code{tooltip-button-2}), enabling safe
re-use of components without ID collisions.

Use \code{alpine_id_ref()} to generate \verb{$id('name')} expression strings for
attributes like \code{for} and \code{id}.
}
\examples{
\donttest{
# Component with two related ID scopes
htmltools::tags$div(
  htmltools::tags$label() |>
    alpine_bind("for", "$id('search-input')") |>
    alpine_text("'Search'"),
  htmltools::tags$input(type = "text") |>
    alpine_bind("id", "$id('search-input')")
) |>
  alpine_id("search-input") |>
  alpine_data(x = "")
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_data_js}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_for}()},
\code{\link{alpine_html}()},
\code{\link{alpine_if}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_teleport}()},
\code{\link{alpine_text}()},
\code{\link{alpine_transition}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
