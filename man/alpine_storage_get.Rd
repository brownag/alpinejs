% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-storage.R
\name{alpine_storage_get}
\alias{alpine_storage_get}
\title{Get Value from localStorage}
\usage{
alpine_storage_get(key, default = NULL)
}
\arguments{
\item{key}{Character string: the localStorage key to retrieve}

\item{default}{Value to use if the key doesn't exist. Can be:
\itemize{
\item \code{NULL} (no default, returns just the getItem call)
\item Character string (quoted in JavaScript)
\item Numeric value
\item Logical value (TRUE/FALSE -> true/false)
\item A list/object (will be serialized using \code{alpine_object()})
}}
}
\value{
An \code{htmlwidgets::JS} object containing a JavaScript expression
that retrieves from localStorage or uses the default.
}
\description{
Helper to create a JavaScript expression that retrieves a value from browser
localStorage with an optional default fallback.
}
\details{
For initializing state with persisted values:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{theme: alpine_storage_get("theme", default = "light")
}\if{html}{\out{</div>}}

When using JSON objects in localStorage (as with \code{alpine_storage_set()}),
parse the result:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Get stored settings or empty object
settings: JS("JSON.parse(localStorage.getItem('settings') || '\{\}')")
}\if{html}{\out{</div>}}
}
\examples{
\donttest{
htmltools::tags$div(
  htmltools::tags$span() |> alpine_text("theme"),
  htmltools::tags$button("Toggle Dark") |> alpine_on("click", "theme = 'dark'"),
  htmltools::tags$button("Toggle Light") |> alpine_on("click", "theme = 'light'")
) |>
  alpine_data(
    theme = alpine_storage_get("theme", default = "light")
  )
}

}
\seealso{
Other Client Storage: 
\code{\link{alpine_storage_set}()}
}
\concept{Client Storage}
