% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_teleport}
\alias{alpine_teleport}
\title{Teleport an element to another part of the DOM}
\usage{
alpine_teleport(tag, selector)
}
\arguments{
\item{tag}{An htmltools tag to teleport. This becomes the content of the template.}

\item{selector}{Character string CSS selector identifying the destination element
(e.g., \code{"body"}, \code{"#modal-container"}, \code{".overlay-root"}).}
}
\value{
An htmltools \verb{<template>} tag with \code{x-teleport} attribute. The content
will be rendered at the selector's target in the browser DOM.
}
\description{
Creates a \verb{<template x-teleport="selector">} wrapper that moves the element's
rendered output to a different DOM node (matched by the CSS selector) rather
than rendering it in place. This is useful for modals, tooltips, and overlays
that need to be rendered outside their parent's stacking context or overflow.
}
\details{
The element is placed in the document where \code{alpine_teleport()} is called
(as a \verb{<template>} tag), but its rendered content appears at the target
selector in the browser. Alpine.js reactive data from the origin component
is still accessible in the teleported content.
}
\examples{
\donttest{
# Teleport a modal to the body element
alpine_teleport(
  htmltools::tags$div(class = "modal",
    htmltools::tags$p("Modal content")
  ),
  selector = "body"
)
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_data_js}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_for}()},
\code{\link{alpine_html}()},
\code{\link{alpine_id}()},
\code{\link{alpine_if}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_text}()},
\code{\link{alpine_transition}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
