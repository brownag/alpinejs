% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magics.R
\name{alpine_dispatch}
\alias{alpine_dispatch}
\title{Dispatch a custom browser event}
\usage{
alpine_dispatch(event, detail = NULL)
}
\arguments{
\item{event}{Character string. The event name to dispatch (e.g., \code{"toggle"},
\code{"item-selected"}, \code{"form-submitted"}).}

\item{detail}{Optional. Event detail data to include. Can be:
\itemize{
\item \code{NULL} (default): no detail payload
\item A named list: serialized to a JSON object
\item A character string: used as raw JavaScript
}}
}
\value{
An \code{htmlwidgets::JS} object containing the \verb{$dispatch(...)} expression.
}
\description{
Generates a \verb{$dispatch('event', detail)} JavaScript expression that dispatches
a custom DOM event from the current element. Other components or elements can
listen for these events using \code{alpine_on()}.
}
\details{
\verb{$dispatch()} emits a \code{CustomEvent} that bubbles up the DOM. Parent elements
can listen for it with \code{alpine_on()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Emitter
alpine_on(button, "click", alpine_dispatch("item-selected", list(id = 1)))

# Listener on a parent element
alpine_on(parent_div, "item-selected.window", "handleSelection($event.detail)")
}\if{html}{\out{</div>}}

Note: events dispatched with \verb{$dispatch} bubble up the DOM. To listen outside
the component tree, use the \code{.window} modifier on \code{alpine_on()}.
}
\examples{
\donttest{
htmltools::tags$button("Select Item") |>
  alpine_on("click", alpine_dispatch("item-selected", list(id = 42, name = "Widget")))
}

}
\seealso{
Other Magic Properties: 
\code{\link{alpine_el}()},
\code{\link{alpine_next_tick}()},
\code{\link{alpine_refs}()},
\code{\link{alpine_store_js}()},
\code{\link{alpine_watch}()}
}
\concept{Magic Properties}
