% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{alpine_filter_buttons}
\alias{alpine_filter_buttons}
\title{Alpine.js Filter/Tab Button Group Component}
\usage{
alpine_filter_buttons(
  choices,
  state_var,
  active_style = NULL,
  inactive_style = NULL,
  button_class = NULL,
  container_class = NULL
)
}
\arguments{
\item{choices}{A named character vector or list where names are button
labels and values are the values to assign to the state variable.
Example: \code{c("All" = "", "Active" = "active", "Archived" = "archived")}
or \code{list("Option 1" = "val1", "Option 2" = "val2")}.}

\item{state_var}{Character string specifying the state variable that
tracks the currently selected value. Example: \code{"selectedFilter"}.}

\item{active_style}{Optional character string with inline CSS for active
button styling. Default applies \verb{background-color: #007bff; color: white;}}

\item{inactive_style}{Optional character string with inline CSS for inactive
button styling. Default applies lighter gray background.}

\item{button_class}{Optional CSS class(es) to apply to all buttons.
Useful for framework integration (e.g., Bootstrap, Tailwind).}

\item{container_class}{Optional CSS class(es) for the button group
container (wrapper div).}
}
\value{
An htmltools tag object (div) containing the button group,
with each button bound to the specified state variable.
Pipe-compatible.
}
\description{
Creates a group of buttons for filtering or selecting options.
Typically used to toggle between different views or filter states.
Uses \code{alpine_on()} for click events and \code{alpine_bind()} for dynamic styling
of the active button.
}
\details{
The component generates a div with multiple buttons, each with:
\itemize{
\item An \code{alpine_on("click", ...)} handler that sets the state variable
\item An \code{alpine_bind()} that dynamically applies active/inactive styling
}

\strong{Styling Strategy:}
\itemize{
\item Provide \code{active_style} for the selected button's appearance
\item Provide \code{inactive_style} for unselected buttons
\item Use \code{button_class} to add base styles (padding, border, cursor, etc.)
\item Combine with CSS frameworks for advanced styling
}

\strong{State Tracking:} The component tracks which button is active by
comparing the state variable value with each button's value. The first
button matching \verb{state_var === value} receives the active styling.
}
\examples{
\donttest{
alpine_filter_buttons(
  choices = c("All" = "", "Active" = "active", "Inactive" = "inactive"),
  state_var = "selectedStatus"
) |>
  alpine_data(selectedStatus = "")
}

}
\seealso{
Other UI Components: 
\code{\link{alpine_accordion}()},
\code{\link{alpine_button}()},
\code{\link{alpine_field}()},
\code{\link{alpine_modal}()},
\code{\link{alpine_stat_card}()},
\code{\link{alpine_tabs}()},
\code{\link{alpine_value_display}()}
}
\concept{UI Components}
