% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tags.R
\name{alpine_tag}
\alias{alpine_tag}
\title{Create HTML tags using Alpine-friendly shorthand}
\usage{
alpine_tag(tag, ...)
}
\arguments{
\item{tag}{Character string naming the HTML tag (e.g., "div", "button", "p"). Supports all standard HTML5 tags.}

\item{...}{Arguments passed to the tag function. Supports regular HTML attributes,
child tags, and Alpine directives via \code{alpine_attr()}. See details.}
}
\value{
An HTML tag object (shiny.tag) with Alpine directives applied.
}
\description{
Convenience wrapper around \code{htmltools::tags} that provides a simpler interface
for creating HTML elements in vignettes and examples.
}
\details{
This function provides a convenient way to create HTML tags without needing
to use \code{htmltools::tags$button()} syntax.

\strong{Alpine directives:} Pass directives using \code{alpine_attr()} to keep them
organized and separate from HTML attributes:

\if{html}{\out{<div class="sourceCode">}}\preformatted{alpine_tag("button", "Click",
  alpine_attr(
    `@click` = "count++",
    `x-show` = "isModal"
  )
)
}\if{html}{\out{</div>}}

Common tags used with Alpine.js examples:
\itemize{
\item \code{alpine_tag("div", ...)}
\item \code{alpine_tag("button", ...)}
\item \code{alpine_tag("input", type = "text", ...)}
\item \code{alpine_tag("p", ...)}
\item \code{alpine_tag("span", ...)}
\item \code{alpine_tag("h1", ...)}, \code{alpine_tag("h2", ...)}, etc.
}
}
\examples{
\donttest{
# Create a simple button
alpine_tag("button", "Click me")

# Nest tags
alpine_tag("div",
  alpine_tag("h2", "Title"),
  alpine_tag("p", "Some content")
)

# Use with Alpine directives
alpine_tag("button", "Toggle",
  alpine_attr(`@click` = "show = !show")
)
}

}
