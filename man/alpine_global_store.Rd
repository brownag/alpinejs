% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/globals.R
\name{alpine_global_store}
\alias{alpine_global_store}
\title{Register a global Alpine.js store}
\usage{
alpine_global_store(name, ...)
}
\arguments{
\item{name}{Character string. The store name used to access it as \verb{$store.name}.}

\item{...}{Named R arguments specifying the initial store state. These are
automatically serialized to JSON. Supports the same value types as
\code{alpine_data()}: logicals, numerics, characters, lists, data.frames,
and raw JavaScript via \code{htmlwidgets::JS()}.

Alternatively, pass a single unnamed \code{htmlwidgets::JS()} value to use
raw JavaScript as the store value.}
}
\value{
An \code{htmltools::tagList} containing a \verb{<script>} tag and the Alpine.js
dependency. Include the result in your page HTML before accessing the store.
}
\description{
Generates a \verb{<script>} tag that calls \code{Alpine.store('name', data)} inside
a \code{document.addEventListener('alpine:init', ...)} listener. Global stores
are accessible from any Alpine component on the page via \verb{$store.name}.
}
\details{
\strong{Accessing the store} in components:
\itemize{
\item Read: \verb{$store.name.property} or use \code{alpine_store_js("name", "property")}
\item Write: \verb{$store.name.property = newValue} in event handlers
}

\strong{Cross-component reactivity:} Because stores are global, any component that
reads a store value will reactively update when that value changes, even when
the change is triggered by a completely different component.
}
\examples{
\donttest{
# R data store
alpine_global_store("theme", mode = "light", fontSize = 16L)

# Store with methods (raw JS)
alpine_global_store(
  "darkMode",
  htmlwidgets::JS("{ on: false, toggle() { this.on = !this.on } }")
)
}

}
\seealso{
Other Global Methods: 
\code{\link{alpine_global_data}()}
}
\concept{Global Methods}
