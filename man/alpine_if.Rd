% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_if}
\alias{alpine_if}
\title{Conditionally add or remove an element from the DOM}
\usage{
alpine_if(inner_tag, condition)
}
\arguments{
\item{inner_tag}{An htmltools tag to conditionally render. This becomes the
single root element inside the template.}

\item{condition}{Character string containing a JavaScript boolean expression.}
}
\value{
An htmltools \verb{<template>} tag with \verb{x-if} attribute. Automatically
includes the Alpine.js dependency.
}
\description{
Creates a \verb{<template x-if="condition">} wrapper that adds or completely removes
its child element based on a JavaScript condition. Unlike \code{alpine_show()} which
uses CSS \code{display: none}, \code{alpine_if()} fully removes the element from the DOM
when the condition is false.
}
\details{
\strong{\verb{x-if} vs \code{x-show}:}
\itemize{
\item \code{alpine_if()} removes the element from the DOM when false (heavier for toggling,
better for elements that are rarely shown or have expensive initialization).
\item \code{alpine_show()} keeps the element in the DOM and toggles \code{display: none}
(lighter for frequent toggling, element state is preserved).
}

\strong{Template restriction:} \verb{x-if} must be used on a \verb{<template>} tag, and the
template must contain exactly one root element. \code{alpine_if()} handles the
wrapping automatically.

\strong{Transition:} \verb{x-if} does not support \code{x-transition}. Use \code{alpine_show()} with
\code{alpine_transition()} if you need enter/leave animations.
}
\examples{
\donttest{
alpine_if(
  htmltools::tags$div("Only rendered when open is TRUE"),
  "open"
)
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_data_js}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_for}()},
\code{\link{alpine_html}()},
\code{\link{alpine_id}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_teleport}()},
\code{\link{alpine_text}()},
\code{\link{alpine_transition}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
