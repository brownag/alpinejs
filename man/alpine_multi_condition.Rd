% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-validators.R
\name{alpine_multi_condition}
\alias{alpine_multi_condition}
\title{Multi-Condition Validator}
\usage{
alpine_multi_condition(..., operator = "&&")
}
\arguments{
\item{...}{Character strings representing JavaScript expressions or results
from other helper functions like \code{alpine_validate_length()} or direct expressions}

\item{operator}{Character string: logical operator to use ("&&" or "||", default: "&&")}
}
\value{
An \code{htmlwidgets::JS} object containing a JavaScript function that
evaluates all conditions and returns the combined result.
}
\description{
Helper to combine multiple validation conditions into a single function.
Useful for complex form validation with multiple interdependent checks.
}
\details{
Conditions are combined with the specified operator. Default is \code{&&} (AND),
meaning all conditions must be true. Use \code{||} (OR) to require at least one.

Conditions can be:
\itemize{
\item Simple field checks: \code{"this.name.length > 0"}
\item Method calls: \code{"this.isValidEmail()"}
\item Expression results: output from \code{alpine_validate_length()}
\item Logical comparisons: \code{"!this.submitted"}
}
}
\examples{
\donttest{
htmltools::tags$div() |>
  alpine_data(
    notifications = TRUE,
    pushNotif = TRUE,
    smsNotif = FALSE,
    hasNotifications = alpine_multi_condition(
      "this.notifications",
      "this.pushNotif",
      "this.smsNotif",
      operator = "||"
    )
  )
}

}
\seealso{
Other Computed Properties: 
\code{\link{alpine_combine_conditions}()},
\code{\link{alpine_compute}()},
\code{\link{alpine_count_filtered}()},
\code{\link{alpine_sum_filtered}()}
}
\concept{Computed Properties}
