% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-js-validators.R
\name{alpine_validate_length}
\alias{alpine_validate_length}
\title{Length Validator Expression}
\usage{
alpine_validate_length(field, min = 1, max = Inf)
}
\arguments{
\item{field}{Character string: name of the field to validate}

\item{min}{Numeric: minimum length (default: 1)}

\item{max}{Numeric: maximum length (default: Inf for no limit)}
}
\value{
A character string containing a JavaScript expression. Can be used
directly in \code{alpine_multi_condition()} or wrapped with \code{alpine_compute()}.
}
\description{
Helper to create a validation condition for string or array length.
Returns a character string containing a JavaScript expression (not a function),
suitable for use with \code{alpine_multi_condition()} or \code{alpine_compute()}.
}
\details{
This generates expressions like:
\itemize{
\item \code{"this.message.length >= 1"} (minimum only)
\item \code{"this.message.length <= 500"} (maximum only)
\item \code{"this.message.length >= 1 && this.message.length <= 500"} (both)
}
}
\examples{
\donttest{
htmltools::tags$div() |>
  alpine_data(
    fullName = "",
    isValidEmail = alpine_validate_email("email"),
    message = "",
    isFormValid = alpine_multi_condition(
      "this.fullName.length > 0",
      "this.isValidEmail()",
      alpine_validate_length("message", min = 1, max = 500)
    )
  )
}

}
\seealso{
Other Input Validators: 
\code{\link{alpine_validate_email}()}
}
\concept{Input Validators}
