% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tags.R
\name{alpine_append_attributes}
\alias{alpine_append_attributes}
\title{Append attributes to an HTML tag}
\usage{
alpine_append_attributes(tag, ...)
}
\arguments{
\item{tag}{An htmltools tag object.}

\item{...}{Attributes to append. Can be:
\itemize{
\item Named arguments: \verb{tag, .attr1 = value1, .attr2 = value2}
\item A single named character vector: \code{c("attr1" = "value1", "attr2" = "value2")}
}}
}
\value{
The modified tag with attributes appended.
}
\description{
Add HTML attributes to a tag object, preserving attribute name case.
Useful for Alpine.js directives and custom JavaScript attributes that
require specific casing (e.g., \code{x-bind:textContent}, \verb{@click}).
}
\details{
\strong{Note:} Most users should use \code{alpine_attr()} with \code{alpine_tag()} for
organized directive assignment. Use this function for low-level attribute
manipulation directly on tags.

Unlike \code{htmltools::tagAppendAttributes()}, this function preserves
the case of attribute names. This is essential for Alpine.js directives
and custom data attributes where case matters.

\strong{Difference from \code{alpine_attr()}:}
\itemize{
\item \code{alpine_append_attributes()} - Direct tag manipulation, low-level
\item \code{alpine_attr()} - High-level, for use with \code{alpine_tag()}
}
}
\examples{
\donttest{
# Low-level attribute manipulation
tag <- htmltools::tags$div("Content")
tag |>
  alpine_append_attributes("x-bind:title" = "myVar", "@click" = "handleClick()")

# With high-level alpine_tag() - preferred
alpine_tag("div", "Content",
  alpine_attr(
    `x-bind:title` = "myVar",
    `@click` = "handleClick()"
  )
)
}

}
\seealso{
Other Tag Utilities: 
\code{\link{alpine_attr}()}
}
\concept{Tag Utilities}
