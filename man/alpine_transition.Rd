% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{alpine_transition}
\alias{alpine_transition}
\title{Apply enter/leave transitions to an element}
\usage{
alpine_transition(
  tag,
  .duration = NULL,
  .delay = NULL,
  .opacity = FALSE,
  .scale = FALSE,
  .scale_value = NULL,
  .origin = NULL,
  .enter = NULL,
  .enter_start = NULL,
  .enter_end = NULL,
  .leave = NULL,
  .leave_start = NULL,
  .leave_end = NULL,
  .cssSelector = NULL
)
}
\arguments{
\item{tag}{An htmltools tag object that also has \code{x-show} (via \code{alpine_show()}).}

\item{.duration}{Numeric. Transition duration in milliseconds (helper mode only).}

\item{.delay}{Numeric. Transition delay in milliseconds (helper mode only).}

\item{.opacity}{Logical. If \code{TRUE}, apply opacity transition only (no scale).
Helper mode only.}

\item{.scale}{Logical. If \code{TRUE}, include a scale transition. Helper mode only.}

\item{.scale_value}{Numeric 0-100. Scale percentage (e.g., \code{80} scales to 80\%).
Only used when \code{.scale = TRUE}. Helper mode only.}

\item{.origin}{Character. Scale origin: \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"},
or space-separated combinations like \code{"top right"}. Only used with \code{.scale = TRUE}.}

\item{.enter}{Character. CSS classes applied for the entire enter phase.}

\item{.enter_start}{Character. CSS classes for the start of enter (before element appears).}

\item{.enter_end}{Character. CSS classes for the end of enter (element fully visible).}

\item{.leave}{Character. CSS classes applied for the entire leave phase.}

\item{.leave_start}{Character. CSS classes for the start of leave.}

\item{.leave_end}{Character. CSS classes for the end of leave (element hidden).}

\item{.cssSelector}{Optional CSS selector for targeting a nested element.}
}
\value{
The modified tag with \code{x-transition} attribute(s) appended.
}
\description{
Adds \code{x-transition} attributes to animate an element when it is shown or
hidden via \code{x-show}. Two modes are supported: the transition helper (using
modifiers for common fade/scale effects) and CSS class mode (for full control
using Tailwind or custom CSS classes).
}
\details{
\strong{Helper mode} (default, using modifiers):
Applies a built-in fade + scale effect. Add modifiers to customize:
\itemize{
\item \code{.opacity = TRUE}: fade only (no scale)
\item \code{.scale = TRUE}: scale only (with optional \code{.scale_value} and \code{.origin})
\item \code{.duration = 500}: 500ms transition
\item \code{.delay = 50}: 50ms delay
}

Helper mode produces a single \code{x-transition[.modifier.modifier...]=""} attribute.

\strong{CSS class mode} (when any of \code{.enter}, \code{.enter_start}, \code{.enter_end},
\code{.leave}, \code{.leave_start}, \code{.leave_end} are provided):
Attaches separate \code{x-transition:phase="classes"} attributes for each phase.
This is used with utility CSS frameworks like Tailwind CSS.

You can mix both modes: use CSS class mode for enter/leave phases alongside
helper mode modifiers where appropriate.
}
\examples{
\donttest{
# Helper mode: fade + scale, 300ms
htmltools::tags$div("Hello") |>
  alpine_show("open") |>
  alpine_transition(.duration = 300)

# Helper mode: opacity only
htmltools::tags$div("Hello") |>
  alpine_show("open") |>
  alpine_transition(.opacity = TRUE)

# CSS class mode (Tailwind)
htmltools::tags$div("Hello") |>
  alpine_show("open") |>
  alpine_transition(
    .enter       = "transition ease-out duration-300",
    .enter_start = "opacity-0 scale-90",
    .enter_end   = "opacity-100 scale-100",
    .leave       = "transition ease-in duration-300",
    .leave_start = "opacity-100 scale-100",
    .leave_end   = "opacity-0 scale-90"
  )
}

}
\seealso{
Other Core Directives: 
\code{\link{alpine_bind}()},
\code{\link{alpine_cloak}()},
\code{\link{alpine_data}()},
\code{\link{alpine_data_js}()},
\code{\link{alpine_effect}()},
\code{\link{alpine_for}()},
\code{\link{alpine_html}()},
\code{\link{alpine_id}()},
\code{\link{alpine_if}()},
\code{\link{alpine_ignore}()},
\code{\link{alpine_init}()},
\code{\link{alpine_modelable}()},
\code{\link{alpine_on}()},
\code{\link{alpine_ref}()},
\code{\link{alpine_show}()},
\code{\link{alpine_teleport}()},
\code{\link{alpine_text}()},
\code{\link{alpine_when}()}
}
\concept{Core Directives}
