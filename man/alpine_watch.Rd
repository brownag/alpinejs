% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magics.R
\name{alpine_watch}
\alias{alpine_watch}
\title{Watch a reactive property for changes}
\usage{
alpine_watch(property, callback)
}
\arguments{
\item{property}{Character string. The name of the reactive property to watch.
Supports dot notation for nested properties (e.g., \code{"user.name"}).}

\item{callback}{Character string. A JavaScript function expression receiving
the new value (e.g., \code{"value => console.log(value)"} or
\code{"(value, oldValue) => doSomething(value, oldValue)"}).}
}
\value{
An \code{htmlwidgets::JS} object containing the \verb{$watch(...)} expression.
}
\description{
Generates a \verb{$watch('property', callback)} JavaScript expression for use in
\code{alpine_init()} or event handlers. When the named property changes, Alpine.js
calls the provided callback with the new value.
}
\details{
Typically used inside \code{alpine_init()} to set up watchers when the component
initializes:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{alpine_init(tag, alpine_watch("query", "value => fetchResults(value)"))
}\if{html}{\out{</div>}}

The callback receives two arguments: the new value and the old value.
For deep watching of nested objects, Alpine.js returns the entire watched
object (not just the changed sub-property).
}
\examples{
\donttest{
htmltools::tags$div(
  htmltools::tags$input(type = "text") |> alpine_model("query")
) |>
  alpine_init(alpine_watch("query", "value => console.log('Query:', value)")) |>
  alpine_data(query = "")
}

}
\seealso{
Other Magic Properties: 
\code{\link{alpine_dispatch}()},
\code{\link{alpine_el}()},
\code{\link{alpine_next_tick}()},
\code{\link{alpine_refs}()},
\code{\link{alpine_store_js}()}
}
\concept{Magic Properties}
