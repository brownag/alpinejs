% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{alpine_config}
\alias{alpine_config}
\title{Control Alpine.js library version and Content Security Policy mode}
\usage{
alpine_config(csp = getOption("alpinejs.use_csp", FALSE), version = "3.15.8")
}
\arguments{
\item{csp}{Logical. If \code{TRUE} (default), uses the CSP-compliant build
(\verb{@alpinejs/csp}) which disallows unsafe-eval and is suitable for strict
Content Security Policies. If \code{FALSE}, uses the standard Alpine.js build
with full JavaScript support. Can be set globally via
\code{options(alpinejs.use_csp = FALSE)}.}

\item{version}{Character string specifying the Alpine.js version.
Defaults to "3.15.8". Should be a valid semantic version available on
the CDN.}
}
\value{
An \code{htmltools::htmlDependency} object containing the Alpine.js script.
Useful for manually including Alpine.js in custom HTML templates. For regular
alpinejs components, Alpine.js is automatically injected.
}
\description{
Configures which Alpine.js build to use: the standard version (with full JavaScript
support) or the CSP-compliant version (compatible with strict Content Security Policies).
Alpine.js is automatically included in generated HTML, but this function lets you
customize the version and security mode.
}
\details{
\strong{Most users don't need to call this directly.} The alpinejs package automatically
injects Alpine.js with sensible defaults. Use this function only if you need to:
\itemize{
\item Switch between standard and CSP-compliant builds
\item Use a specific Alpine.js version
\item Manually attach Alpine to HTML templates
}

\strong{Automatic Injection:} Alpine.js is automatically injected by functions like
\code{alpine_data()}, \code{alpine_on()}, and other modifiers. You typically do not need
to call this function.

\strong{Manual Use Cases:}
\itemize{
\item Changing the Alpine.js version globally
\item Switching CSP mode for your application
\item Attaching Alpine.js to custom HTML templates
}

\strong{CDN:} Alpine.js is fetched from jsDelivr (https://www.jsdelivr.com/):
\itemize{
\item \strong{CSP mode} (\code{csp = TRUE}):
\verb{https://cdn.jsdelivr.net/npm/@alpinejs/csp@VERSION/dist/cdn.min.js}
\item \strong{Standard mode} (\code{csp = FALSE}):
\verb{https://cdn.jsdelivr.net/npm/alpinejs@VERSION/dist/cdn.min.js}
}

\strong{CSP Limitations:} The CSP build disallows certain JavaScript patterns
(functions, closures) to comply with strict Content Security Policies.
See \href{https://alpinejs.dev/advanced/csp}{Alpine.js CSP documentation}.
}
\examples{
\donttest{
# Use CSP-compliant build (default)
dep <- alpine_config(csp = TRUE)

# Use standard build with full JavaScript support
dep <- alpine_config(csp = FALSE)

# Use a different Alpine.js version
dep <- alpine_config(version = "3.14.0")
}

}
\concept{Advanced Configuration}
